# KCL Function

The KCL (Kubernetes Configuration Language) function uses KCL for type-safe, validated templating.

## Installation

Install the KCL function:

```bash
kubectl apply -f function-install.yaml
```

## Basic Usage

KCL provides type-safe templating with built-in validation.

See `kcl-examples/` for examples.
